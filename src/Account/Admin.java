package Account;

// Admin.java
public class Admin extends Account {
    public Admin(String id, String pin) { // constructor
        super(id, pin); // Call the parent class's constructor with the id and pin parameters.
    }
}
